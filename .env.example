# PostgreSQL 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=yewon042135

# 서버 설정
PORT=3000
NODE_ENV=development

# 주의사항
# 1. DB_NAME을 실제 데이터베이스 이름으로 변경하세요
# 2. DB_USER와 DB_PASSWORD를 PostgreSQL 계정 정보로 변경하세요
# 3. 이 파일은 .gitignore에 추가하여 Git에 커밋하지 마세요
